# Project Instruction

Use [.github/instructions.md](instructions.md) as the canonical GitHub/Codex
instruction file for this repository.

Hard branch rule: new unrelated work must start on a dedicated feature branch
from `dev`. If the current branch already contains another feature, ask whether
to commit/merge/create a new branch or whether the new work truly belongs to the
current feature.
